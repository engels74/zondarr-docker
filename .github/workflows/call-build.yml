name: call-build

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - 'workflows'

jobs:
  call:
    uses: engels74/base-image/.github/workflows/build-on-call.yml@workflows
    secrets: inherit
